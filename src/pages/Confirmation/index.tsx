import { useState, useEffect } from 'react';
import { Input, Button, Tooltip, message } from 'antd';
import styles from './index.less';

const Confirmation = () => {
  return <div className={styles['main-container']}>Confirmation</div>;
};
Confirmation;
export default Confirmation;
